# Codex 只读联动：接入与候选核验

日期：2026-08-28

结论：默认关闭的联动源码候选已实现，472 项测试及最新原生窗口检查通过；真实额度、当前任务的处理中及增量更新已有证据。状态包的临时接收范围已获同意，正文只在本机提取后丢弃。真实任务终态、并发和深链点击仍未验，安装版及分享包未改，不称完整实机交付。

## 已确认

- 工作树为现有 `codex/light-companion`，开始时干净，未在主分支开发。
- `npm test -- --test-reporter=dot` 实际仍输出默认报告，退出码 0：217 项通过、0 失败；这是原有功能基线，不是新功能验收。
- 本机 Codex CLI 版本为 `0.150.0-alpha.8`。
- `codex app-server daemon version` 返回默认 `app-server-control/app-server-control.sock` 不存在。未启动、重启或修改共享服务。
- 系统打开文件清单显示，桌面应用使用已有的 `.codex/ipc/ipc.sock`。探测先检查 socket 及父目录属于当前用户、非符号链接且不可被其他用户写入，再连接；没有创建 socket、目录或修改权限。
- 使用独立的 `emotion-ball-desktop-pet` 客户端标识，只发送 `initialize`。收到成功响应；不冒用已有窗口、任务或执行器身份，不声明任务所有权。
- 十五秒被动监听只收到一次 `thread-stream-following-changed`，包含会话标识、主机标识、是否跟随等元数据；没有收到可用于判断处理中、等待或完成的状态。输出仅记录字段类型和事件计数，不记录会话标识值、标题或正文。
- 结束时主动断开探测连接，探测进程退出码 0；没有留下监听服务。

## 代码侧证据与限制

只读检查了本机安装包，不修改安装资源。当前构建中的相关位置：

- `.vite/build/src-LXsvCZ9X.js`：IPC 使用长度前缀帧；`initialize` 分配独立客户端标识；`thread-stream-state-changed` 当前版本为 11，跟随通知版本为 1。
- `webview/assets/app-initial-CpK4W6kT.js`：`sendConversationSnapshot` 从 `threadStore.getConversation` 取会话状态，`sendSnapshot` 将整个状态对象作为 `conversationState` 发送。
- 同一状态模型包含已加载的轮次/条目历史，而非仅运行标志。现有发送方法没有「只发送状态字段」参数。
- 应用工具插件要求执行器提供当前任务元数据；不能把任务内工具当作任意独立桌宠都能长期调用的接口，也不伪造执行器元数据绕过其边界。

构建文件名只是本次定位证据，不能硬编码成跨版本产品接口。上述代码说明了传输范围；未实际请求、接收或保存完整会话快照，不能据此宣称实时任务提醒已经验证。

## 未执行的操作

- 未发送 `thread-stream-following-changed: following=true`，未请求完整快照/完整历史。
- 未调用恢复任务、开始轮次、发送消息、审批、归档、打断或额度重置。
- 未读取凭据文件，未修改 Codex 配置、数据库或安装包；未增加后台服务。
- 未安装桌宠候选版，未改变正式版设置，未更新分享 ZIP。

## 获授权后的补充核验

用户已同意：开启后本机临时接收状态包，只提取进展，正文不落盘、不上传；分享用户开启前同样说明并确认。

- 在本次正在进行的用户任务上发送只读跟随通知，收到版本 11 的 snapshot，`threadRuntimeStatus.type = active`。
- `turns` 可为空；本机使用 `turnHistory.history.entitiesByKey`，当前轮次 `status = inProgress`。不能只检查旧版 turns 数组。
- 收到连续版本的 patches；没有打印/写入标题、正文、凭据。输出仅为类型、状态、字段名与计数。
- 结束时发送 following=false 并断开；两次短暂探测均已正常退出，无残留监听。
- 仍未主动创建、恢复、审批、发送或中断任务。此前「未执行」清单描述授权前的探测，不代表此后的跟随请求未发送。
- 本轮重新读取 `account/read` 和 `account/rateLimits/read` 成功；额度仍返回真实窗口、已用比例和重置时间。诊断仅打印字段名，没有打印账户或额度数值。
- `thread/list` 使用 `limit:20, sortKey:updated_at, archived:false, sourceKinds:[], useStateDbOnly:true` 成功；返回二十条并有后续游标，因此只能表述为最近任务的局部视图。标题字段是 `name`，不能以 `preview` 代替。
- 独立 stdio 探测进程已终止；未启动默认共享 daemon。
- 首轮新连接模块真实试跑：额度四个有效窗口、任务连接初始成功；随后因大帧保护断开，不能据初始成功称稳定通过。
- 仅记录帧长度的定向核验发现 28,603,365 与 198,189,762 字节的大包，总接收约 228MB；超过 64KiB 的帧未解码正文，只按帧长流式跳过。探测结束已取消跟随并退出。后续需要验证逐任务大包隔离，不提高主线程全文解析上限。

## 连接模块候选回验

- 提交 `432aba9`：五个只读模块及对应测试，未接入菜单或安装版。
- 大包隔离修复后，根任务用真实 `createCodexConnection` 再运行 21 秒：额度四个有效窗口；任务保持 connected/PARTIAL_STATE。十九条可入选任务中一条 active、十八条 unknown，unknown 中两条明确 STATE_TOO_LARGE；未把无快照的任务当成结束。
- 第十二秒只刷新任务元数据，之后连接仍在；关闭后探测进程正常退出。此轮 Node 进程采样峰值 RSS 约 130MiB，仅代表该次运行，不代表全部设备性能。
- 大包主体不进入 JSON 解析；只读取有界协议头并流式丢弃。正常包仍按版本 11 校验。等待/结束/失败真实变化、界面动作与安装验收仍未完成。
- 连接模块经规格和质量两轮复核，最终 `6d94382` 全量 350 项通过：补齐无有效快照时退出已连接状态、账号切换清旧状态、跨账号并发列表读取隔离、轮次排序歧义持续保护，以及两类等待列表超限后的独立恢复。根任务另外重跑状态/流/连接 102 项通过。以上属于代码和隔离测试证据，不替代未发生的真实终态验收。
- 为落实两路独立重连，`dc318c2` 增加 `retry({quota,tasks})`，`db06b32` 补齐混合并发恢复：额度单路重试不动健康或永久不可用的任务通道；任务单路重试不重读账号额度。连接 33 项、全量 369 项通过（含新设置测试），旧连接迟到结果和并发恢复均有定向回归。

## 界面接入前的实机基线

提醒层截至 `4891359` 已通过规格与质量复核；根任务全量 424 项通过。独立复核重放了终态关闭、展示许可期间状态改变、真实连接编排切换账号后继续刷新三项边界。默认关闭、两路独立退避、阈值去重、五秒合并、三十秒间隔、六十秒过期及安全菜单均为代码候选能力，尚未接入界面或安装版。

- 在独立临时设置目录运行 `npm run smoke`，退出码 0，包含 `PET_SMOKE_OK`、六类动作、80/120/180/260 四尺寸、双屏边界、双击、拖动、气泡回应、固定灰白色、80×80 睡眼及 Zzz。
- 截图保存在 `/tmp/emotion-ball-codex-baseline.NLvUu0`，根任务已目视查看普通互动气泡；该目录为临时证据，不打包、不提交。此轮还没有接入 Codex 界面，不算新功能验收。
- 只读检查本机 Codex 前端代码可见其自身构造 `codex://threads/<id>`；应用已注册 `codex` scheme。这里只验证链接格式的来源，尚未真实点击新气泡跳转。

## 界面接入与故障复核（源码候选）

- `ff28a6c`、`7489616`、`3078911`、`fc2ac77` 接入受控气泡、动作归属、菜单开关、独立授权确认和显式打包清单。打包暂存目录实际加载七个 Codex 模块及验收助手成功；没有新增依赖、Codex 程序、用户账号或状态快照。
- 规格审发现页面重载后旧消息会消耗提醒。`9e0f5ed` 增加独立页面标识，`cfa5536` 补齐「清理当前提醒触发通知时，未加载完成不得提前发新标识」；独立重放重载和崩溃链路通过。页面更新不重建 Codex 连接。
- 质量审注入磁盘 ENOSPC，发现原关闭流程先保存设置，写失败会继续读取；开启写失败则出现假勾选。`0edcbb0` 改为关闭先释放全部资源再保存，开启保存失败仍关闭；固定提示在开关旁，关闭后也可见，明确未保存时重启可能恢复。独立回放连接关闭、计时器归零及迟到消息丢弃通过。
- 主代理在 `0edcbb0` 独立执行全量 472 项测试，0 失败、0 跳过；候选源码质量复核通过。测试结论不替代下面的原生窗口和真实任务状态门槛。

### 原生窗口记录

- `4f48439` 一轮完整 `npm run smoke` 退出码 0：原有 38 个身体动作案例、7,936 帧、双屏边界与四尺寸全部通过；新增 80/120/180/260 联动气泡、动作完整结束、真实「知道啦」按钮 IPC、处理中不弹泡、关闭清理、用户双击抢占及不抢焦点检查也通过。
- 该轮证据位于 `/tmp/emotion-ball-codex-recheck.TS7ygT`；主代理目视查看四种气泡与 80×80 球体。字体均 14px，文案实际为一至两行，文字与按钮在气泡边界内。额度和任务数据明确为模拟连接，只有窗口、页面、预加载、动作与 IPC 使用真实应用，不能冒充真实任务终态验收。
- 中途两轮分别在原有摸头、保持清醒关闭后的睡眠检查失败；后一次 `cfa5536` 复跑在原有 jelly 气泡检查失败。根因尚未定位，不能因另一次成功而称这些问题已修复。诊断仅增加有界事件与时序记录，不修改原有规则、冷却或断言；不移动实际鼠标。
- 最终代码 `c4bba9b` 再次完整执行 `npm run smoke`，退出码 0，全部原有标记及 `PET_CODEX_SIZE_80/120/180/260_OK`、`PET_CODEX_SIMULATED_OK`、睡眼/Zzz、弹跳均通过。原有身体动作 38 案例/7,836 帧、两块显示器；四档联动文案为一至两行、14px，文字与按钮均在气泡边界内。主代理重新目视完成与额度气泡、80×80 球体及睡眼/Zzz。
- 最终临时证据目录 `/tmp/emotion-ball-codex-trace.QVlTQN`，包括 `codex-native-results.json`、`motion-verification.json`、`presentation-events.json` 和 PNG。诊断增量独立验证失败后恢复包装/监听、最多 1,000 条、测试正文/账号标记不保存；不在正常运行执行，也不把诊断产物打包。最终全量再次执行 472/472，0 失败、0 跳过。

### 尚未验收与交付范围

- 真实 waiting/completed/failed/interrupted 的状态转换、多个真实任务同时提醒，以及点击「去看看」实际到达正确任务尚未核验。测试中的对应动作和按钮不能代替这些真实验收；没有创建收费测试任务。
- 首次开启的隐私说明、取消/快速开关/晚回调、磁盘写失败、页面重载等由真实主进程/渲染代码加模拟系统边界验证；并未声称所有异常都在实机发生。
- 本轮只交付源码候选并同步 `codex/light-companion`。没有构建新的 DMG、替换 `/Applications/球球桌宠.app`、更新分享 ZIP、合并主分支或发布 Release。
- 现有安装包 `app.asar` 的 SHA-256 保持 `d47f8f168df9f918605a6ffd498ee7b80f3e04cef76dc5d1ab8dbcb5f4238f06`；0.2.2 分享 ZIP 保持 `61bf6fc96c780cb00ea6c0b01b93fcd836382b57a58b6165b5070e86d4c60e58`。

## 用户要求继续验证后的复跑（2026-08-28）

- 在同一源码版本 `adbebb7`、两个不同临时设置目录，连续完整运行两轮 `npm run smoke`，均退出码 0；未修改代码、规则、冷却时间或断言。两轮都包含原有摸头、睡眠、六种双击动作及对应气泡、双屏边缘、四尺寸、Codex 模拟提醒、关闭清理和用户互动优先检查。
- 第一轮证据 `/tmp/emotion-ball-codex-followup.kQjaH3`：原有身体动作 38 案例 / 7,883 帧，229 条展示事件记录。第二轮 `/tmp/emotion-ball-codex-repeat.AHacue`：38 案例 / 6,388 帧，236 条展示事件记录。主代理查看完成、多任务等待、额度、失败气泡及 80×80 睡眼/Zzz 截图；四档文字为 14px、一至两行，按钮与文字均在气泡边界内。
- 随后重新运行全量测试，472/472 通过，0 失败、0 跳过。历史摸头、睡眠和 jelly 气泡偶发失败本轮没有复现；这只能增加回归证据，不能据此认定根因已定位或问题已根治。
- 本轮真实只读订阅仅选择当前任务，约 20 秒内收到 8 次状态回调，其中包括 `active` 初始状态及后续更新；四个真实额度窗口均有有效剩余比例及重置时间。本轮没有出现 waiting/completed/failed/interrupted 转换，不将 active 更新当作终态验收。未记录账号标识、额度具体数值或聊天正文。
- 另用实际进程与套接字对象复核关闭：本次探测自己创建的进程已退出、自己创建的套接字已销毁；未停止其他既有 Codex 进程。
- 尝试通过受支持的原生界面工具查看 Codex 时，被明确以安全原因拒绝；没有改用其他界面自动化绕过。故「点击后实际到达正确任务」仍需用户在 Codex 界面确认。只读连接证据不替代界面跳转证据。
- 已询问是否允许创建两条会使用少量额度的专用测试任务；本轮尚未获得该项授权，未创建、发送、中断或故意制造真实任务失败。真实终态、并发提醒及正确跳转继续标为未验收，不称完整联动验收通过。
- 本轮仅追加验收记录；没有更换本机应用或分享包。重新计算两者 SHA-256，与上节记录完全一致；没有合并主分支或发布 Release。
