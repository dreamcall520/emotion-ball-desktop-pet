# 靠边轮廓与眨眼节奏精修

按用户截图反馈实施，功能提交 `d55b860`。

- 靠边清醒眨眼从 6–14 秒改为 2.5–5 秒；沿用自然眨眼动画和微呼吸。
- 原正中裁半圆的上下直角，改为贴边圆润曲线；露出的头部略收短，双眼缩小、下移并完整露出。左右轮廓对称，展开恢复原圆形。
- 修复进入另一侧收起时沿用旧朝向的问题；直接由靠边侧设置内侧朝向，覆盖锁屏/恢复消息先后到达，活动采样仍不重画冻结帧。

## 验证

- 全量单元测试 877/877 通过；独立只读复核通过。
- 最终打包 app 专项 `PET_EDGE_SMOKE_OK`：五尺寸 × 左右两侧，双眼实际路径采样点均在新头部轮廓内且不被屏边裁掉；展开恢复原路径；拖动、悬停、双屏、隐藏、额度胶囊回归通过。
- 收起连续 15.5 秒 SVG 采样观察到 6 次眼睛闭合周期、120 个不同呼吸帧，采样期间双眼始终位于头部轮廓内；暂停后冻结。
- 外接矩形的空白角在倾斜眼睛下不代表实际溢出，几何检查最终按每只眼睛真实路径的 64 个采样点判断，并保留真实截图人工查看。
- 已查看 60/80/260px 左右真实截图；当前范围是源码和本机实际渲染核验，最终美感仍以用户体验为准。
- 包内 60 个 JS/CSS/HTML 与最终源码一致（仅体验 APP_NAME 不同），严格临时签名通过。

## 交付

- 已更新并正常打开 `/Users/allan/Documents/ChatGPT/F03｜球球桌面宠物/球球靠边体验/球球靠边体验版.app`。
- 原体验设置逐字节保留；更新时设置 SHA-256 为 `d445b53b1f3e2c72110fc1ba7c2f5c1be5e13dd464d3d310ece4cbc3ea5731d3`。
- 新 app.asar SHA-256：`15c017a0778512ebc8d8e303e3b0c35b92f14ea0d1685df005b37afe58ee0605`。
- 上一体验包保留在交付目录 `验证记录/backup-27549c7/球球靠边体验版.app`。
- 正式应用 asar 仍为 `bdd049f1f913e92d58ec123d67c99ffcf02e82a46761b0e5f04c6db0ade37c9c`；本轮未公开发布。
- 专项证据：`desktop-pet/output/edge-polish-packaged-final`、`edge-polish-manifest.json`；交付目录“验证记录/靠边外观精修”有副本。

本轮按改动范围运行完整单元测试与靠边原生专项；未重复上一轮已通过的全部动作原生长回归。专项中的鼠标、额度及通知时钟为测试输入，SVG、窗口、样式与 IPC 为真实打包应用。
