# 0.3.13 Apple 芯片版交付核验

## 本次范围

- 接入确认的 V5 摸头侧躺、放下转面、唤醒绕小圈及日常文案，保留原有双击动作。
- 按所在显示器工作区判断内侧朝向，屏幕中部使用缓冲区；眼神、单侧互动、睡眠符号及思考光迹一起适配。鼠标眼神跟随仍按真实屏幕方向响应，文字不镜像。
- 思考改为头顶短光迹，约 6 秒展示、25–35 秒安静；使用独立、不抢焦点、鼠标穿透的透明窗口，不改变球窗或对白窗尺寸。头顶有边界或对白障碍时向侧方避让，不从眼睛出现。
- 一轮任务完成时随机使用原生跳跃或渐变彩带，文案与动作在同一条提醒内固定，不因刷新重抽。
- 不改变通用 Codex 额度统计、用户偏好、Intel 分支或其安装包。独立预览文件未覆盖。

## 检查结果

- `npm test`：806 项通过，0 失败、0 跳过；`git diff --check` 通过。
- 源码完整真实窗口检查：原有六种动作、三个新互动、五档尺寸、浅深额度卡片、长对白、双屏及负坐标边界、睡眠和窗口归位通过，输出 `PET_SMOKE_OK`。
- 最终冻结包精准真实窗口检查：Codex 模拟联动、思考光迹实际位移及眼睛避让、周期结束隐藏、任务提醒和长标题、额度外观/五档尺寸/负坐标、睡眠及边界检查通过，输出 `PET_SMOKE_OK`。
- 56 个打包运行文件与最终源码逐字节一致。冻结包、DMG 内应用和正式安装应用的 `app.asar` 哈希一致。
- DMG 校验、只读挂载、ZIP 完整性、ZIP 内 DMG 哈希校验通过。应用版本为 0.3.13、架构 arm64，严格临时签名校验通过。
- 正式安装版已启动，实际窗口来自 `/Applications/球球桌宠.app`；替换前后及启动后所有设置逐项一致。

验收中发现并复现了旧脚本在唤醒绕圈过程中比较额度卡片静态位置的问题。只修正验收前提：观察唤醒开始、等待宿主和页面均结束且球窗精确归位，再清除暂用唤醒对白；没有放宽原位置断言或修改额度定位逻辑。修正前源码精准模式同样失败，修正后源码及最终冻结包均通过。

## 可核对产物

| 产物 | SHA-256 |
| --- | --- |
| 正式应用 `app.asar` | `bdd049f1f913e92d58ec123d67c99ffcf02e82a46761b0e5f04c6db0ade37c9c` |
| `球球桌宠-0.3.13-Apple芯片.dmg` | `929311c8c25973e171216fd3755f973ebce7c6cff720b19ce3ac5b783fa4adb1` |
| `球球桌宠-0.3.13-Apple芯片-分享用.zip` | `c02510c44140e1f31ce96f0f87b6358d6605fc99f366b27ea3e8e7199f64cdc6` |

旧 0.3.12 应用及最新设置已放入废纸篓的独立恢复目录，另有本地旧版 ZIP 备份；源码、旧分享包和 Intel 产物未删除。

## 证据边界

- Codex 场景自动验收使用模拟任务状态，但渲染、窗口、预加载、消息通道、对白和动作均使用真实应用代码；不把模拟任务检查称为全部真实任务来源兼容性认证。
- 仅验证当前 Apple 芯片 Mac；不代表 Intel 实机或所有 macOS 版本验收通过。
- 未使用 Developer ID 签名或 Apple 公证，首次安装提示及许可边界保持原说明。
