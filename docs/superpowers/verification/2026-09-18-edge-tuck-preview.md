# 球球靠边与隐藏：本地体验版验收

## 交付范围

基于 main 762b28b（0.3.13），在 codex/edge-tuck 独立工作区实现。功能提交 dab1b6f，菜单与恢复修复 6c7bd0a，连续点击时序修复 f59e603。

- 真正拖动到左右边缘 16px 内吸附；鼠标离开后露半身，进入可见半身展开，移开约 650ms 后收回。
- 收起期间透明部分鼠标穿透。完整原生方窗始终留在工作区内，半身效果由内容裁切实现。
- 显示额度卡、普通右键菜单及 Codex 结果菜单期间保持展开；拖离边缘取消吸附。
- 菜单支持靠左、靠右、离开边缘、暂时隐藏和显示球球；点击对应菜单栏图标可恢复。
- 收起/隐藏时关闭现有气泡、额度卡和思考光迹，后台 Codex 状态继续更新；展开显示最新额度，已显示旧气泡不重播。未展示且仍有效的提醒沿用既有过期和去重规则。
- 靠边与隐藏为会话状态，保存的位置始终完整可见。重启显示完整球球。

## 已确认验证

- 最终源码单元测试：852 项全部通过，0 失败、0 跳过。
- 规格独立复核通过；代码质量复核通过，修复了结果菜单自动缩回、恢复时残留拖动，以及连续点击时旧确认包误取消新拖动。
- 源码完整原生回归输出 PET_SMOKE_OK；包含原六种双击动作、摸头/放下/唤醒、睡眠、气泡、五尺寸、额度及模拟 Codex 联动。
- 最终打包应用的靠边专项输出 PET_EDGE_SMOKE_OK：60/80/120/180/260 五尺寸 × 左右两侧、悬停/移开、真实 Chromium pointer 拖动、普通点击不吸附、隐藏后后台额度/任务更新与恢复均通过。
- 两块当前物理显示器的左右靠边检查通过；包括工作区 (-704,-1692,3008,1692) 的负坐标副屏。窗口 bounds 保持在所在工作区。
- 已查看最终包 80px 左侧半身、副屏右侧半身截图；截图前等待实际 CSS 位移到终点，并在捕获后再次检查位置，避免将过渡首帧误作终态。
- 最终交付包完整原生回归：已通过，输出 PET_SMOKE_OK；直接运行交付目录中的冻结 app。
- 交付 app 严格临时签名核验通过。包内 53 个 JS/CSS/HTML 文件已核对，唯一代码差异是为隔离体验版而替换 APP_NAME；package 名称及 bundle ID 也独立。

## 本地体验产物

- 应用：/Users/allan/Documents/ChatGPT/F03｜球球桌面宠物/球球靠边体验/球球靠边体验版.app
- 说明：同目录 使用说明.md。
- 已正常启动交付目录的体验版，进程及打开的 GPU/浏览器缓存路径均位于独立的“球球靠边体验版”配置目录；首次体验放在主屏左侧。
- Bundle ID：local.xiaokun.emotionball.edgepreview；基线版本 0.3.13，不表示新公开版本。
- 体验包 app.asar SHA-256：48ecc530748bb8365d985ce23bbdd545dddc03e9ee57d2690e3427765ee8f2c6。
- 正式安装包 app.asar 前后保持 bdd049f1f913e92d58ec123d67c99ffcf02e82a46761b0e5f04c6db0ade37c9c。main 与 Intel worktree 均未改动。

## 证据与边界

证据目录为本工作区 desktop-pet/output/edge-tuck-packaged-final（截图、逐帧几何及汇总）、edge-packaged-full-final（完整回归）、edge-preview-manifest.json（包内对比）。原始日志在收尾时复制到交付目录“验证记录”。

鼠标位置与 Codex 额度/任务由测试夹具提供，窗口、页面、IPC、Chromium 输入、双屏摆放和显示/隐藏均走真实应用代码；不把这些检查称作全部真实 Codex 任务兼容性认证。

本次仅交付 Apple 芯片本地体验版；未替换正式应用、未改共享安装包或 Intel 产物、未推送远端或发布 Release。
