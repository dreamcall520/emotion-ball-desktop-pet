# 靠边轻陪伴：独立体验版验收

功能提交 `27549c7`，基于 `codex/edge-tuck` 的第一阶段体验版继续迭代。已更新并正常启动原交付路径的“球球靠边体验版”，通过真实右键菜单设为靠左收起；界面确认露出半身。

## 行为

- 靠边清醒时每约 6–14 秒眨眼，呼吸幅度 0.007；睡眠时闭眼微呼吸，无 zzz。隐藏、暂停和锁屏静止，展开立即恢复正常状态。
- 短句约 3–6 分钟一次，持续 4.5 秒；额度约 5–10 分钟一次，持续 6 秒。首次短句 60–90 秒、额度 30–60 秒后具备展示条件，共享最少 90 秒间隔。
- 两种提示共用玻璃小胶囊，沿用现有外观设置；鼠标穿透、不抢焦点、不展开球球。沿用互动气泡、Codex 联动及额度显示开关；额度只取现有有效主周期，不弹过期百分比。
- 展开、隐藏、锁屏、关闭设置、重载/关闭窗口立即收回，旧窗口迟到回调不能重新显示。

## 本次证据

- 全量单元测试 875/875 通过；将周窗口文案统一为“周额度”后，涉及的 113 项再次通过。
- 独立需求审查与代码质量审查均 PASS。
- 源码真实 Electron 靠边专项通过。
- 最终打包 app 的靠边专项输出 `PET_EDGE_COMPANION_OK` 与 `PET_EDGE_SMOKE_OK`：真实 SVG 连续采样 15.5 秒观察到眨眼和呼吸；暂停后帧不变；左右/浅深两种提示不截字、不抢焦点；隐藏及恢复通过。
- 沿用的专项同时检查五尺寸、左右两侧、真实 Chromium 拖动、悬停和两块显示器（含负坐标副屏）。
- 最终打包 app 的完整原生互动回归输出 `PET_SMOKE_OK`，覆盖旧动作、睡眠、对白、额度卡及模拟 Codex 联动。
- 包内 60 个 JS/CSS/HTML 与源码逐字节核对一致，仅允许体验版 APP_NAME 不同；严格临时签名核验通过。
- 体验版现有设置文件更新前后 SHA-256 一致：`ceb3e6e719d89cde07a0f73176c4a70eb6bb8c8b2087d98b940c7d0d8153da53`。大小、位置、Codex 与额度开关保留。
- 更新后从真实菜单读到 Codex 额度和任务均已连接；使用真实菜单执行靠左收起，UI 确认半身显示。未把模拟验收数值当作用户账户额度。

## 交付与边界

- 体验 app：`/Users/allan/Documents/ChatGPT/F03｜球球桌面宠物/球球靠边体验/球球靠边体验版.app`
- app.asar SHA-256：`679eb768a9684d7b9f414f91aba86ad39bc0b3588fac7287265dfa470eef548a`。
- 上一阶段 app 保留在同目录 `验证记录/backup-34dbc2d/球球靠边体验版.app`。
- 正式安装版 app.asar 仍为 `bdd049f1f913e92d58ec123d67c99ffcf02e82a46761b0e5f04c6db0ade37c9c`；main 与 Intel 工作区干净。没有推送、公开发布或更换正式安装版。
- 证据：工作区 `desktop-pet/output/edge-companion-packaged`、`edge-companion-full`、`edge-companion-manifest.json`；交付目录“验证记录/靠边轻陪伴”保留副本。
- 专项的额度与鼠标、通知时钟为可控测试输入；窗口、SVG、IPC、布局与显隐运行真实打包应用。正常体验版使用原有真实数据通路与实际时间。
