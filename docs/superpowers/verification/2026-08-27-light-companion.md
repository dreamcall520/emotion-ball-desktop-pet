# 轻陪伴 0.2.0 验收与续接

## 当前状态

实现位于 `codex/light-companion` 分支。本地候选应用已生成，**尚未替换 `/Applications/球球桌宠.app`，也未更新 GitHub 或分享 ZIP**。最终包的真实窗口验收被 Mac 锁屏中断，已请求用户手动解锁；不能绕过锁屏或修改系统设置。

## 已确认

- 无新增依赖；眼神、工作活跃近似、5/10/15 分钟休息、手动睡眠、摸头、拖放、离线气泡和两个开关已接入。
- 116 项 Node 测试通过。覆盖状态阈值、手动优先、四角/负坐标、对白冷却和过期按钮、锁屏/挂起交错、动作重播与取消、气泡异常恢复；新增行为均先红后绿。
- 独立需求复核通过；独立代码复核提出的三项重要问题已修复并复核清零（复核专项 82/82 通过）。
- 源码版真实 Electron 验收曾完整通过：实际左右眼睛位移、模式转换、摸头/拖放、休息按钮、四角气泡、实际系统空闲接口、80×80 睡眼/Zzz 和原生窗口弹跳。
- 阈值验收使用可控活动包注入，不声称已实际无人操作等待 15 分钟。外接屏负坐标为规则测试，不声称已接上另一块屏幕验收。
- 最后的“再来一次重播、休息取消排队点击、气泡故障恢复”已通过真实引擎/渲染逻辑回归；修改后的最终包仍需解锁后再跑原生检查。
- `--user-data-dir` 实际隔离有效；macOS `/var` 与 `/private/var` 指向同一临时目录，运行时用 realpath 核对，不能直接比较字符串。用户原设置保留。
- 打包清单包含全部运行文件；生成的是 Apple Silicon、本机临时签名应用，未作 Apple 公证。

## 文件

- 候选应用：`dist/球球桌宠-darwin-arm64/球球桌宠.app`
- 源码版已查看截图：`desktop-pet/output/companion-0.2.0/source-preview/`
- 原应用和设置备份：`desktop-pet/output/backups/2026-08-27-v0.1.0/`
- 本机原安装版实际回读为 **0.1.0**；不要用公开仓库 0.1.1 版本代替本机事实。

## 解锁后继续

1. 从本工作树运行 `npm test` 和 `npm run smoke`。
2. 对候选应用设置 `PET_SMOKE_APP_PATH` 再运行 `npm run smoke`，可用 `PET_SMOKE_ARTIFACT_DIR` 与 `PET_SMOKE_SCREENSHOT` 保存截图；必须通过 `PET_BUBBLE_REPLY_OK`（包括两个真实按钮）及全部标记。
3. 再次核验签名、app.asar、版本；确认备份有效，退出旧桌宠后替换安装，保留原尺寸/位置设置。
4. 启动安装版，从实际菜单验证开关、点击查看气泡和动作，回读版本并确认没有错误。
5. 更新本文与计划的完成状态。除非用户另行授权，继续不发布、不推送、不改分享包。
