# 球球桌宠 0.3.14 正式交付核验

## 发布范围

- 用户授权替换正式应用、同步 GitHub 代码并更新官网下载地址。
- 本轮公开交付 Apple 芯片版 0.3.14；Intel 官网下载保留 0.3.13。
- 应用代码冻结提交及版本标签：`daf5e3e0e011e1be6f94b1fbfa011f09ed480204` / `v0.3.14`。
- Intel 源码已同步至 `codex/intel-package` 的 `99591b2`，本地 0.3.14 候选不对外分发。

## Apple 包与本机安装

- 主线合并后 877/877 单元测试通过，0 失败、0 跳过。
- 最终正式包完整原生回归 `PET_SMOKE_OK`，靠边专项 `PET_EDGE_SMOKE_OK`；覆盖五档尺寸、左右靠边、含负坐标双屏、悬停展开、拖离、隐藏/恢复、静息眨眼呼吸与额度胶囊。
- 第一次完整回归的合成按下期间收到实体鼠标移动，窗口被拖离预期锚点，触发朝向断言；保留失败记录。随后顺序运行相同包，完整回归和靠边专项通过，未修改代码或放宽断言。
- 包内 65 个源文件与冻结提交逐字节一致；生成的 package.json 和应用版本均为 0.3.14。全部 15 个 Mach-O 为 arm64，严格 ad hoc 签名验证通过，最低 macOS 12.0。
- DMG 校验通过；挂载后应用签名及 app.asar 与已验证包一致。镜像包含应用、Applications 链接、安装说明、LICENSE、NOTICE.md；ZIP 只有一个同版本 DMG，解压内容逐字节一致。
- `/Applications/球球桌宠.app` 已替换并启动，原设置逐字节保留；真实菜单已确认靠左、靠右、隐藏入口及 Codex 额度连接。
- 旧 0.3.13 应用和设置保存在项目交付目录 `发布备份/0.3.13-before-0.3.14-20260918`。体验版退出，保留原应用和独立设置。
- 未使用 Apple Developer ID 签名或苹果公证。

## 产物校验值

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| app.asar | — | `54220eef5c5449a98a9c26d586f1c0f3ec319e0822fcf345f332670c4f44865c` |
| Qiuqiu-0.3.14-macOS-arm64.dmg | 118230734 | `99ecdc2df8828eb0e492934512d2bb5cdf6d6cbae28462a4f0c20cfb11b905d1` |
| Qiuqiu-0.3.14-macOS-arm64-share.zip | 116798868 | `476a548dc6204ebfc67275b96327c7ed0683e03b1c0fb21198f8ee8772d1399c` |

## Intel 边界

Intel 候选通过 881 项单元测试、x86_64 架构、签名和安装文件校验，但原始完整运行回归的动作采样时长断言，以及靠边专项的切换首帧眼形断言未通过。诊断样本后续帧恢复正常，仍不能视为首帧问题已修复。此次不发布该候选，官网保留已公开的 0.3.13 链接及真实 Intel Mac 未验收说明。

完整 Intel 证据见 `codex/intel-package` 分支的 `docs/superpowers/verification/2026-09-18-v0314-intel-x64-package.md`。

## 公开交付

- 代码 main、功能分支和 `v0.3.14` 标签已推送并按 SHA 回读。
- 官网本地 1440/390 下载区与更新日志布局通过，无横向溢出；版本分流最终静态复核为 Apple 0.3.14、Intel 0.3.13；最近 3 条、历史 5 条。
- [Release v0.3.14](https://github.com/dreamcall520/emotion-ball-desktop-pet/releases/tag/v0.3.14) 已公开并设为最新版本；仅包含以上两个 Apple 附件，GitHub 返回的大小和 SHA-256 均与本地一致。
- 从公开直链完整重新下载 ZIP 和 DMG，两份文件的大小、SHA-256 均与本机验证的产物一致。保留 `public-download-verification.json` 及下载文件。
- 官网提交 `9b44dbd279593b5aa7c8f51c4fadca31b0fd01bf` 已在 GitHub Pages 构建成功；线上首页与本地源码逐字节一致，SHA-256 为 `c98bedb189a97f60c74ffddd3da51375ed151e91e5a047c5ce3b5e29b9fec4c9`。
- 线上两个 Apple 入口均指向 0.3.14 ZIP，Intel 入口保留 0.3.13 ZIP 并核对 HTTP 200；未将待验收 Intel 包上传至公开 Release。

## 本地证据

- 功能 worktree：`desktop-pet/output/v0314-apple-full-isolated`、`v0314-apple-edge`、`v0314-release`。
- 安装记录：项目交付目录 `发布备份/0.3.13-before-0.3.14-20260918/installation-record.json`。
- 官网本地验证：项目交付目录 `球球靠边体验/验证记录/官网0.3.14/output/playwright`。
